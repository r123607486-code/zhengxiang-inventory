# 正享庫存系統 — 專案工作規則

正式版 repo：`r123607486-code/zhengxiang-inventory`
線上網址：https://r123607486-code.github.io/zhengxiang-inventory/

---

## 一、最高原則：沒有提到的部分，一個字都不准動

這是本專案的第一鐵則。過去曾因為「順手重構」造成多個已完成的功能在使用者不知情的狀況下消失
（庫存校正、單價／稅別／小計、業務下拉選單、表格固定高度捲動），修復耗費大量時間。

### 必須遵守

1. **只改被明確要求的部分。** 其他既有程式碼、註解、格式、空行，全部原樣保留。
2. **禁止「重寫整份檔案」式的改法。** 即使覺得舊寫法不好、命名不一致、有更漂亮的架構，
   除非使用者明確要求重構，否則不准動。
3. **改動前先完整讀過該檔案**，確認自己知道每一段在做什麼，再動手。
   不確定某段程式是幹嘛的 → 問使用者，不要猜、不要刪。
4. **推送前自我檢查：** 這次改動有沒有動到「使用者沒提到的東西」？有的話就是錯的，要改回來。

### 如果真的需要重構

不能自己決定。必須先做到以下三件事，經使用者確認後才能動手：

1. 列出「這次會動到哪些檔案、哪些功能」的完整清單
2. 明確說明「哪些現有功能會被保留、怎麼保留」
3. 重構完成後，逐一比對重構前的功能清單，確認一個都沒少

---

## 二、程式要用「功能」分檔，改一個功能只動一個檔案

目的：使用者要調整某個功能時，不需要重讀或重推整個系統。

### 目前的檔案分工（動手前先確認自己改對檔案）

| 檔案 | 負責範圍 |
|---|---|
| `firebase-config.js` | Firebase 連線設定 |
| `core.js` | 共用核心：全域狀態、共用工具函式、登入／登出、分類切換、分頁建立、Firestore 監聽器啟動 |
| `modal-shared.js` | 共用彈窗開關 |
| `tire-inventory.js` | 輪胎：庫存查詢、庫存總表、儲位／價格編輯、叫貨、匯出 |
| `tire-transactions-orders.js` | 輪胎：進銷貨管理、庫存校正、訂單管理、我的訂單 |
| `kyb-inventory.js` | KYB：庫存查詢、庫存總表、儲位編輯、叫貨、匯出 |
| `kyb-transactions-orders.js` | KYB：進銷貨管理、庫存校正、訂單管理、我的訂單 |
| `locations-users.js` | 儲位管理、使用者管理 |
| `data-import.js` | 資料匯入 |
| `backup-restore.js` | 備份還原 |
| `handover-backup.js` | 交接用完整備份匯出 |
| `index.html` | 所有頁面結構、表格欄位標題、按鈕 |
| `style.css` | 全站樣式 |
| `manifest.json` / `sw.js` / `icon-*.png` | 手機 App（PWA）設定，與業務邏輯無關 |

### 新增功能時

- 找得到對應的功能檔 → 加在那個檔案裡
- 屬於全新的功能領域 → **開一個新檔案**，不要塞進 `core.js`
- 只有「輪胎和 KYB 都要用到的共用邏輯」才放 `core.js`

### 不要做的事

- 不要把某個功能的程式碼拆散在多個檔案
- 不要因為「順手」把 A 功能的程式碼搬到 B 檔案
- 不要在 `core.js` 塞單一功能專用的邏輯

---

## 三、輪胎與 KYB 必須對稱

兩邊是各自獨立的資料（`items`/`kybItems`、`transactions`/`kybTransactions`…），
但**功能應該一致**。

- 改了輪胎某個功能 → 主動問使用者「KYB 那邊要不要一起改」
- 不要只改一邊就當作做完

---

## 四、推送與驗證流程

1. **動任何檔案前先列計畫，等使用者確認再執行。**
2. 刪除、覆寫、改名檔案前一律先問。
3. 推送 GitHub 後，**一定要重新抓回檔案內容確認**（過去發生過推送時打錯跳脫字元、多打括號，
   整個網站掛掉的狀況）。JS 檔可用 `node --check` 驗證語法。
4. 改了 `.js` 或 `.css` → **一定要同步更新 `index.html` 裡對應的 `?v=日期` 版號**，
   否則使用者手機／瀏覽器會繼續讀舊的快取版本，改了等於沒改。
5. 需要放圖的地方**留空白給使用者**，不要自動生成或裁切圖片。

---

## 五、資料安全

- Firestore 的欄位名稱不可隨意更名（例：`salesperson`、`requestedByName`、`batchDate`、
  `adjustSign`、`taxMode`）。改名會導致既有資料讀不到，等同資料遺失。
- 任何會清除／覆寫資料庫的功能（資料匯入、清除資料、還原備份），
  必須保留現有的二次確認提示，不可簡化。
- 若未來要幫其他公司做類似系統：**程式碼可以重用，但 Firebase 專案（資料庫）絕對不可共用。**

---

## 六、同時開多個對話視窗的風險

過去曾發生「同一個 repo 在兩個對話視窗被同時修改」，兩邊沒有共同記憶，
後推送的那次直接覆蓋掉前一次的成果，造成功能消失。

- 盡量不要同時在兩個視窗處理同一個 repo
- 在新視窗接續工作前，先讓 AI 重新讀取目前 repo 的實際內容，不要憑舊印象直接改

---

## 七、回應方式

- 簡潔直接，不要冗長解釋
- 不確定的地方直接說「不確定」，不要猜
- 預設交付格式：docx 或 xlsx
